server:
  port: 8080
  servlet:
    context-path: /api
spring:
  application:
    name: assistant-gpt
    version: @project.version@
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/assistant_gpt?useSSL=false&serverTimezone=UTC
    username: username
    password: password